Place a VITS COQUI-TTS-compatible model here, and its config file. Rename the model to "model.pth", and the config file to "config.json"
If the model has more pieces, drop them in the main MonikA.I. folder for now.
